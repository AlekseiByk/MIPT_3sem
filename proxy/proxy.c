#include "proxylib.h"

int main (int argc, char ** argv) 
{
	if ((argc < 2) || (argc > 2)){
		printf ("not right program start\n%s <number of children>\n", argv[0]);
		exit (1);
	}

	errno = 0; 
	

	return 0;
}

